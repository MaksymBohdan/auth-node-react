const pass = 'react-node-auth';
const userName = 'react-node-auth';
const confiq = {
  port: '4040',
  url: `mongodb+srv://${userName}:${pass}@cluster0-mvo7w.mongodb.net/test?retryWrites=true&w=majority`
};

module.exports = confiq;
